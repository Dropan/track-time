import React from 'react';

const TimeControlPanel = () => {
  return (
    <div>
        <button>Play</button>
        <button>Pause</button>
        <button>Stop</button>
        <button>+/-</button>
    </div>
  );
};

export default TimeControlPanel;